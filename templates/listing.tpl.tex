% mainfile: ../../../../master.tex
\subsection{@taskTitle}
% The part of the label after the colon must match the file name. Otherwise,
% conditional compilation based on task labels does NOT work.
\label{task:@label}
\tags{@tags}
%\authors{}
%\files{}
%\persons{}

% Listing
\begin{lstlisting}[language=r, caption=Python dictionary for amino acids labels, label=lst:python_dict_for_aa]
library(ggplot2)
library(phyloseq)

\end{lstlisting}